"use strict"; //treat all JS code as newer version

// alert(2+3) //we are using nodejs, not browser

console.log(2+3)
//code readability should be high
console.log('ankur')

let name = "ankur"
let age = 30
let isLoggedIn = false
let state; //undefined type declaration

/* primitive datatypes
number => 2 to the power 53
bigint
string => ""
boolean => true/false
null => standalone value
undefned => variable declared but value has not been assigned
symbol => used for checking uniqueness



object
*/
console.log(typeof age);

console.log(typeof isLoggedIn);

console.log(typeof null); //null is an object type

console.log(typeof undefined); //undefined is itself a type